# AOP v2.0 플랫폼 운영 가이드

- **플랫폼 개요**: 학업 방식 검사 v2.0 (AOP: Academic Operation Profile)
- **개발**: 바이브스타틱스 현용찬(교육학박사)

---

## 1) 문항/판본 구조

- **전 판본 동일 구조**: 총 **37문항(1~37)**, 표시 순서 고정(셔플 없음)
- **판본(edition)**: `elementary`(초등), `school`(중고등), `univ`(대학생), `adult`(성인)
- **채점 포함**: 1~28번
- **채점 제외(저장만)**:
  - 29~33번(쿤 패러다임 5문항, 성찰 코칭 질문)
  - 34~37번(점검 문항: 주의집중/허위·미화)
- **리커트(6점)**:
  - `elementary`: 1 전혀 아니다 … 6 정말 그렇다
  - 그 외: 1 전혀 그렇지 않다 … 6 매우 그렇다

---

## 2) 채점 알고리즘(4대 축)

### 2.1 축 정의(문항 번호)

- **IE(즉흥 실행)**: 1~7
- **SA(체계 분석)**: 8~14
- **WD(위임·위축)**: 15~21
- **IO(영향 지향)**: 22~28

### 2.2 점수 산출 공식

각 축 점수는 해당 문항들의 **문항평균(1~6)** 입니다.

\[
\text{score(axis)} = \frac{1}{n}\sum_{i \in \text{axis\_items}} a_i
\]

- \(a_i\): i번 문항 응답(1~6)
- \(n\): 축별 문항 수(현재 7)
- 산출 후 소수 둘째 자리로 반올림(예: 4.285 → 4.29)

### 2.3 채점 제외 규칙

- `SCORING_EXCLUDED_IDS = [29,30,31,32,33,34,35,36,37]`
- 29~33(쿤) 및 34~37(점검) 문항은 **점수/차트/해석에 반영하지 않고**, 원점수로만 저장합니다.

---

## 2.5 파일럿 통계 요약(해석 모델 근거)

아래 요약은 현재까지의 파일럿 데이터에서 관찰된 경향을 바탕으로, 전문가/연구자가 결과를 **과도하게 단정하지 않고** 읽기 위한 참고 메모입니다.

- **IE–SA 관계**: 완전한 직교라기보다 **약한 음의 상관(예: \(r \approx -0.205\))**이 관찰될 수 있습니다. 즉, 서로 대립 축으로 단정하기보다는 “보완적 기질(동시에 높아질 수도 있음)”로 읽는 것이 안전합니다.
- **WD(부담 신호) 주효과**: 파일럿에서는 WD가 IO보다 **IE(+) / SA(-)**의 영향과 더 일관되게 연결되는 경향이 확인되었습니다. 해석에서는 WD를 “성격 결함”이 아니라 **현재 부담/과부하 신호**로 다루는 것이 적절합니다.
- **중앙형의 처리**: IE·SA가 모두 중앙(2.5~4.0) 범위인 집단이 적지 않게 나타날 수 있어(예: 약 36% 내외), 이 경우 “주력 기질”로 단정하기보다 **WD(안정/과열)로 1차 트리아지** 후, 쿤(29~33) 문항을 성찰 질문으로 연결하는 방식이 유용합니다.

### 2.4 결과 차트 스펙트럼 라벨(좌↔우)

결과 화면의 각 축은 “저점=결함”이 아니라 **좌·우 모두 강점**으로 읽도록, 아래 스펙트럼 라벨을 함께 표시합니다.

| 척도(기호) | 좌측 라벨(저점, 1.0~2.5) | 중앙(2.5~4.0) | 우측 라벨(고점, 4.0~6.0) |
| :--- | :---: | :---: | :---: |
| **즉흥 실행 (IE)** | **계획 정돈 / 신중** | [ 균형 / 조율 ] | **즉흥 실행 / 돌파** |
| **체계 분석 (SA)** | **맥락 감각 / 유연** | [ 균형 / 조율 ] | **체계 분석 / 구조** |
| **영향 지향 (IO)** | **내면 몰입 / 탐구** | [ 상황 협력 ] | **영향 주도 / 리드** |
| **위임·위축 (WD)** | **주체 안정 / 견딤** | [ 일상 부하 ] | **과부하 위축 / 외주** |

---

## 3) 신뢰도/검증 알고리즘(v2.0+)

### 3.1 일관성 검증(Consistency → attention_ok)

- `CONSISTENCY_PAIRS`로 지정된 문항쌍의 점수 차이가 너무 크면 비일관으로 봅니다.
- 판정:
  - \(|a-b| > 4\) 이면 `attention_ok = false` (차이 4까지 허용)
  - 모든 쌍이 통과하면 `attention_ok = true`

현재 설정:

- (2, 4) / (8, 11) / (15, 16) / (22, 25)
- `CONSISTENCY_DIFF_THRESHOLD = 4`

### 3.2 점검 문항(지정 응답/허위·미화)

- **주의집중 점검(지정 응답)**: 점검 문항에서 “몇 번을 선택해 달라”는 지시를 따르는지 확인합니다.
  - 현재 설정: `ATTENTION_EXPECT = { 34: 3, 35: 4 }`
- **허위·미화 점검**: “단 한 번도 거짓말한 적이 없다” 같은 비현실적 문항에 고점(5~6)을 주면 `lie_ok`에 반영합니다.
  - 현재 설정: `LIE_IDS = [36, 37]`, `LIE_HIGH_THRESHOLD = 5` (즉, 5~6이면 의심)

### 3.3 극단반응 검증(Extreme responding → lie_ok)

- 1~28번 응답에서 **1 또는 6이 지나치게 많이 반복**되면 비정상 응답으로 봅니다.
- 판정:
  - 1점 개수 ≥ 24 **또는** 6점 개수 ≥ 24 이면 `lie_ok = false`
  - 그렇지 않으면 `lie_ok = true`

현재 설정:

- `EXTREME_RESPONSE_COUNT_THRESHOLD = 24`

### 3.4 최종 신뢰도(reliable)

연구 초기 단계에서는 스크리닝을 너무 엄격하게 적용하면 표본을 왜곡할 수 있으므로, 아래 **3가지 스크린 중 2개 이상 통과**를 `reliable`로 봅니다.

- 스크린 3종:
  1) **일관성**(attention_ok)
  2) **점검 문항 지정 응답**(ATTENTION_EXPECT)
  3) **허위·미화/극단반응**(lie_ok)
- 판정:
  - `reliable = (통과한 스크린 개수 >= 2)`

---

## 4) DB(Supabase) & CSV 스키마

### 4.1 Supabase 테이블 구조(핵심)

#### `access_codes`
- `id` (uuid, PK)
- `code` (text, unique) — 예: `AOP-OPEN`, `EXP-XXXXXX`
- `kind` (text) — `public` / `expert`
- `label` (text) — 기관/전문가 메모
- `active` (boolean)
- `created_at` (timestamptz)

#### `sessions`
- `id` (uuid, PK)
- `access_code_id` (uuid, FK → access_codes.id)
- `result_no` (text, unique) — 결과번호 `AOP-XXXX`
- `display_name` (text)
- `gender` (text)
- `grade` (text)
- `major` (text)
- `region` (text)
- `edition` (text) — `univ` / `school` / `adult` / `elementary`
- `answers` (jsonb) — q1~q37 원점수 저장
- `scores` (jsonb) — ie/sa/wd/io 축 점수
- `attention_ok` (boolean)
- `lie_ok` (boolean)
- `reliable` (boolean)
- `consent_version` (text)
- `created_at` (timestamptz)

### 4.2 CSV 내보내기(관리자)

관리자 화면에서 CSV를 내려받을 때 헤더는 아래 순서로 생성됩니다.

- `result_no`, `created_at`, `edition`, `access_code`, `code_kind`
- (상담용 CSV만) `display_name`
- `gender`, `grade`, `major`, `region`
- `attention_ok`, `lie_ok`, `reliable`
- `ie`, `sa`, `wd`, `io`
- `q1` … `q37` (응답 최대 문항번호까지 자동 확장)

---

## 5) 전문가/기관 추천인 코드 체계(EXP-)

플랫폼은 “전문가/기관 그룹”을 **코드 기반(access_codes)**으로 분리합니다.

- **공개 응시**: `AOP-OPEN` (kind=`public`)
- **전문가/기관**: `EXP-XXXXXX` (kind=`expert`)

### 운영 방식

- 수검자는 입장 코드로 진입합니다.
- 세션은 `sessions.access_code_id`로 **어떤 코드(그룹)로 들어왔는지**가 고정 연결됩니다.
- 관리자는 전문가 코드를 발급하고(라벨에 기관명/상담자명 기록), 필요 시 비활성화할 수 있습니다.
- CSV 내려받기 시에도 `access_code` / `code_kind`가 함께 포함되어, 기관별/전문가별 데이터 묶음이 가능합니다.

---

## 6) 유지보수(Maintenance) 모드

스키마·문항 인덱스 튜닝 기간에는 일반 수검자 진입을 차단할 수 있습니다.

- **기본 차단**: 점검 안내 카드 표시 후 `/take`, `/result`, `/` 등 일반 접근 차단
- **우회 허용**:
  - `?mode=dev` (내부 테스트)
  - `#/admin` (관리자)

운영 중에는 `MAINTENANCE_MODE` 토글로 제어합니다.

